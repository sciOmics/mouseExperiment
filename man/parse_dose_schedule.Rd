% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dose_schedule.R
\name{parse_dose_schedule}
\alias{parse_dose_schedule}
\title{Parse a dosing schedule from free text}
\usage{
parse_dose_schedule(text, groups = NULL)
}
\arguments{
\item{text}{Character scalar. Either \code{"1, 5, 9, 13"} (applies to all groups in
\code{groups}) or per-group lines such as \code{"DrugA: 1,5,9,13"}, one per line or
separated by \verb{;}. Group names are matched case-insensitively.}

\item{groups}{Optional character vector of treatment-group names present in
the data. Used to expand the bare form and to validate the per-group form.}
}
\value{
A data frame with \code{Treatment} and \code{Day}, carrying a \code{scope} attribute
of \code{"shared"} or \code{"per_group"}; or \code{NULL} when nothing was supplied.
Unmatched group names raise a warning and are dropped — silently ignoring
them is the failure mode this package has repeatedly found (§K.2).
}
\description{
Accepts either a bare list of days applying to every treated group, or one
line per group. Blank input returns \code{NULL}, which callers treat as "no
annotation requested" rather than an error.
}
\examples{
parse_dose_schedule("1, 5, 9, 13", groups = c("Control", "DrugA"))
parse_dose_schedule("DrugA: 1,5,9\nCombo: 1,5", groups = c("DrugA", "Combo"))
}
