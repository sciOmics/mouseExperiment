% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bayesian_config.R
\name{tg_mcmc}
\alias{tg_mcmc}
\title{MCMC sampler configuration object}
\usage{
tg_mcmc(
  chains = 4L,
  warmup = 1000L,
  iter = 500L,
  seed = 42L,
  backend = c("rstan", "cmdstanr")
)
}
\arguments{
\item{chains}{Number of MCMC chains (default 4).}

\item{warmup}{Warm-up iterations per chain (default 1000).}

\item{iter}{Post-warm-up iterations per chain (default 500).}

\item{seed}{PRNG seed (default 42).}

\item{backend}{"rstan" (default) or "cmdstanr". Resolved by
\code{resolve_brms_backend()} before being passed to \code{brms::brm()}, so
if \code{cmdstanr} is requested but unavailable the call falls back to
\code{rstan} rather than erroring.}
}
\value{
An object of class \code{tg_mcmc} (a tagged list).
}
\description{
Bundles the four-or-five MCMC-related arguments accepted by every
\verb{bayesian_*} entry point into a single named list. CODE_REVIEW.md
Round 2 D.2.
}
\details{
The individual \code{n_chains}, \code{n_warmup}, \code{n_iter}, \code{seed}, and \code{backend}
arguments on every Bayesian function continue to work; when an \code{mcmc = tg_mcmc(...)} argument is supplied, its fields take precedence.
}
\examples{
tg_mcmc()
tg_mcmc(chains = 2, iter = 1000, backend = "cmdstanr")
}
\seealso{
\code{\link[=tg_priors]{tg_priors()}}
}
