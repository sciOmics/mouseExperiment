% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/permutation_trajectory.R
\name{trajectory_permutation_test}
\alias{trajectory_permutation_test}
\title{Randomisation test for a treatment x time interaction}
\usage{
trajectory_permutation_test(
  df,
  time_column = "Day",
  volume_column = "Volume",
  treatment_column = "Treatment",
  id_column = "ID",
  cage_column = "Cage",
  transform = c("log", "sqrt", "none"),
  spec = perm_spec(),
  verbose = FALSE
)
}
\arguments{
\item{df}{Long data frame, one row per observation.}

\item{time_column, volume_column, treatment_column, id_column}{Column names.}

\item{cage_column}{Cage column. Required when \code{spec$unit == "cage"}.}

\item{transform}{Applied to the response before modelling: \code{"log"} (default),
\code{"sqrt"}, or \code{"none"}. Should match what the primary analysis used.}

\item{spec}{A \code{\link[=perm_spec]{perm_spec()}} object.}

\item{verbose}{Print progress.}
}
\value{
A list with:
\item{p_value}{Two-sided permutation p-value, \verb{(1 + #\{T* >= T\}) / (1 + n_perm)}
so it is never exactly zero.}
\item{observed_statistic}{The LRT statistic on the real labels.}
\item{perm_statistics}{The \code{n_perm} null-distribution values.}
\item{unit, n_units, n_perm}{What was permuted and how many of them.}
\item{exhaustive}{\code{TRUE} when every distinct assignment was enumerated, so
the p-value is exact rather than a Monte-Carlo estimate. Enumeration kicks
in automatically for small designs, where it is both exact and usually
cheaper than sampling.}
\item{min_attainable_p}{The smallest p-value the design can produce
regardless of effect size. This is \strong{not} \code{1 / n_distinct_assignments}:
the statistic is invariant to relabelling the groups, so with \code{g}
equal-sized groups \verb{g!} assignments tie with the observed one and the floor
is \verb{g! / n_distinct_assignments}. With 6 cages in 3 arms of 2 that is
\code{6/90 = 0.067}, so p < 0.05 is unattainable — worth knowing before
interpreting a null result as evidence of no effect.}
\item{n_distinct_assignments}{Multinomial count of distinct labellings.}
\item{n_distinct_statistics}{Distinct statistic values, i.e.
\code{n_distinct_assignments} divided by the label symmetry.}
\item{lrt_p_asymptotic}{The chi-square LRT p-value, for comparison. A large
discrepancy against \code{p_value} is a signal that the asymptotic approximation
is not to be trusted on this dataset.}
}
\description{
Tests the global null that treatment has no effect on the growth trajectory, by
re-randomising treatment labels the way the experiment did and recomputing the
interaction statistic each time.
}
\section{Why use this}{

The \code{lme4} path reports p-values that depend on the Kenward-Roger /
Satterthwaite denominator-df approximation and on normally-distributed random
effects. Both are approximations, and the df approximation is known to be
anti-conservative at the group sizes typical of these studies (8-10 animals per
arm). This test relies on neither: under the null created by the actual
randomisation, every relabelling is equally likely, so the reference
distribution is exact up to Monte-Carlo error in \code{n_perm}.
}

\section{What is permuted, and why it matters}{

A permutation test is only valid if the permuted labels are exchangeable under
the null, which means the permutation has to mirror the randomisation. With
\code{unit = "mouse"} the treatment labels are shuffled across animals, holding each
animal's whole trajectory together. With \code{unit = "cage"} whole cages are
relabelled, so co-housed animals stay together.

The choice is consequential. If cages were the unit of assignment, the number of
exchangeable units is the number of \emph{cages}, not animals — a 6-arm study with 2
cages per arm has 12 units, not 48, and the test's resolution reflects that. The
returned \code{min_attainable_p} makes this explicit rather than leaving it to be
discovered.
}

\section{Test statistic}{

The likelihood-ratio statistic comparing the full model
(\code{response ~ treatment * time + (1 | id)}) against the reduced model without the
interaction (\code{response ~ treatment + time + (1 | id)}), both fitted by ML. Large
values indicate the trajectories diverge. The statistic is only used to rank
permutations, so its own distributional properties do not matter — that is the
point of the approach.
}

\examples{
\dontrun{
data(master_synthetic_data)
trajectory_permutation_test(
  master_synthetic_data,
  spec = perm_spec(unit = "mouse", n_perm = 199, seed = 1)
)
}
}
\seealso{
\code{\link[=perm_spec]{perm_spec()}}, \code{\link[=tumor_growth_statistics]{tumor_growth_statistics()}}
}
