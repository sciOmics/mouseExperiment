% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bayesian_therapeutic_window.R
\name{bayesian_twm_from_data}
\alias{bayesian_twm_from_data}
\title{Fit and Compute Bayesian Therapeutic Window from Raw Data}
\usage{
bayesian_twm_from_data(
  tg_df,
  bw_df,
  time_column = "Day",
  treatment_column = "Treatment",
  id_column = "ID",
  cage_column = NULL,
  reference_group = NULL,
  volume_column = "Volume",
  tg_transform = c("log", "sqrt", "none"),
  necrotic_column = NULL,
  weight_column = "Weight",
  bw_transform = c("none", "log", "sqrt"),
  prior_strength = c("skeptical", "weakly_informative", "informative", "diffuse",
    "manual"),
  n_chains = 4L,
  n_iter = 2000L,
  seed = 42L,
  include_cage_effect = TRUE,
  noise_floor = 1,
  tg_endpoint_day = NULL,
  plots = TRUE,
  verbose = FALSE
)
}
\arguments{
\item{tg_df}{Data frame of tumor-growth observations. Must contain
\code{time_column}, \code{treatment_column}, \code{id_column}, and
\code{volume_column}.}

\item{bw_df}{Data frame of body-weight observations. Must contain
\code{time_column}, \code{treatment_column}, \code{id_column}, and
\code{weight_column}.}

\item{time_column}{Name of the study-day column present in both data frames.
Default \code{"Day"}.}

\item{treatment_column}{Name of the treatment-group column present in both
data frames. Default \code{"Treatment"}.}

\item{id_column}{Name of the animal-ID column present in both data frames.
Default \code{"ID"}.}

\item{cage_column}{Name of the cage column present in both data frames.
\code{NULL} (default) omits cage random effects.}

\item{reference_group}{Name of the vehicle/control group. \code{NULL}
(default) auto-detects from factor levels.}

\item{volume_column}{Name of the tumor-volume column in \code{tg_df}.
Default \code{"Volume"}.}

\item{tg_transform}{Variance-stabilising transform applied to tumor volume
before fitting. One of \code{"log"} (default), \code{"sqrt"},
\code{"none"}.}

\item{necrotic_column}{Optional column in \code{tg_df} flagging necrotic
observations. Passed to \code{bayesian_tumor_growth()}.}

\item{weight_column}{Name of the body-weight column in \code{bw_df}.
Default \code{"Weight"}.}

\item{bw_transform}{Variance-stabilising transform applied to body weight
before fitting. One of \code{"none"} (default), \code{"log"},
\code{"sqrt"}.}

\item{prior_strength}{Prior strength used for both sub-models. One of
\code{"skeptical"} (default), \code{"weakly_informative"},
\code{"informative"}, \code{"diffuse"}, \code{"manual"}.}

\item{n_chains}{Number of MCMC chains. Default \code{4L}.}

\item{n_iter}{Iterations per chain (including warm-up). Default \code{2000L}.}

\item{seed}{Random seed for reproducibility. Default \code{42L}.}

\item{include_cage_effect}{Logical. Include cage-level random intercept in
both sub-models when \code{cage_column} is supplied? Default \code{TRUE}.}

\item{noise_floor}{Minimum absolute weight-loss percentage used as the TWM
denominator (passed to \code{bayesian_therapeutic_window()}). Default
\code{1.0}.}

\item{tg_endpoint_day}{Study day for TGI computation. \code{NULL} (default)
uses the last observed day in the TG model data.}

\item{plots}{Logical. Return \pkg{ggplot2} plot objects? Default \code{TRUE}.}

\item{verbose}{Logical. Print progress messages? Default \code{FALSE}.}
}
\value{
The same named list as \code{\link{bayesian_therapeutic_window}},
with an additional element \code{tg_result} and \code{bw_result} holding
the full outputs of the two sub-model fits.
}
\description{
Convenience wrapper that fits a \code{\link{bayesian_tumor_growth}} model
and a \code{\link{bayesian_body_weight}} model from raw data frames in a
single call, then passes both fitted results to
\code{\link{bayesian_therapeutic_window}} to compute the posterior TWM
distribution. All arguments that control the fitting of both sub-models are
exposed directly; see the individual function documentation for details.
}
\seealso{
\code{\link{bayesian_therapeutic_window}},
\code{\link{bayesian_tumor_growth}}, \code{\link{bayesian_body_weight}}
}
