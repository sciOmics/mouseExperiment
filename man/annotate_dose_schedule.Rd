% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dose_schedule.R
\name{annotate_dose_schedule}
\alias{annotate_dose_schedule}
\title{Annotate a time-axis plot with a dosing schedule}
\usage{
annotate_dose_schedule(
  p,
  schedule,
  style = c("auto", "rug", "band"),
  colors = NULL,
  max_marks = 8L,
  label = "Dosing"
)
}
\arguments{
\item{p}{A ggplot whose x aesthetic is study day.}

\item{schedule}{A data frame from \code{\link[=parse_dose_schedule]{parse_dose_schedule()}}, or \code{NULL} to return
\code{p} unchanged.}

\item{style}{\code{"auto"} (default), \code{"rug"}, or \code{"band"}.}

\item{colors}{Optional named colour vector keyed by treatment, so marks match
the curve colours. Groups absent from it fall back to grey.}

\item{max_marks}{Passed to \code{\link[=dose_schedule_style]{dose_schedule_style()}}.}

\item{label}{Axis-side label for the strip; \code{NULL} for none.}
}
\value{
The plot with the annotation added.
}
\description{
Draws dosing days in a strip below the plotting panel: down-triangles for an
intermittent schedule, a filled window for a continuous one. Groups with
different schedules are stacked, one row each, so the annotation carries the
same information the removed \code{plot_treatments()} panel did without a second
plot to keep aligned.
}
