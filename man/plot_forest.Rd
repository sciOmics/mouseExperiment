% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_forest.R
\name{plot_forest}
\alias{plot_forest}
\title{Create Forest Plot for Hazard Ratios or Effect Sizes}
\usage{
plot_forest(
  results_df,
  group_order = NULL,
  title = "Forest Plot of Hazard Ratios",
  show_text = TRUE
)
}
\arguments{
\item{results_df}{Data frame containing results (hazard ratios or effect sizes).}

\item{group_order}{Optional vector specifying the order in which groups should appear (from top to bottom).}

\item{title}{Optional title for the plot. Use "Forest Plot of Effect Sizes" for tumor growth data
or "Forest Plot of Hazard Ratios" (default) for survival data.}

\item{show_text}{Logical, whether to display values with confidence intervals and p-values below group names. Default: TRUE.}
}
\value{
A ggplot2 object with the forest plot visualization.
}
\description{
Creates a forest plot showing hazard ratios or effect sizes with confidence intervals.
Points represent estimates, and horizontal lines represent 95\% confidence intervals.
This function works with both survival analysis results (hazard ratios) and
tumor growth statistics (effect sizes).
}
\examples{
# First run survival analysis
results <- survival_statistics(df = tumor_data)

# Then create forest plot with default ordering
plot_forest(results$results)

# Custom group ordering
plot_forest(results$results, 
           group_order = c("Control", "Drug A", "Drug B"))
}
