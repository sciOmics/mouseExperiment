% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/permutation_trajectory.R
\name{perm_spec}
\alias{perm_spec}
\title{Permutation-test configuration}
\usage{
perm_spec(unit = c("mouse", "cage"), n_perm = 999L, seed = NULL)
}
\arguments{
\item{unit}{The unit of randomisation — \strong{what was actually randomised to
treatment}, not what is convenient to permute.
\describe{
\item{\code{"mouse"}}{(default) Individual animals were assigned to treatment.
Whole mouse trajectories are relabelled. This is the common case.}
\item{\code{"cage"}}{Cages were assigned to treatment, so every animal in a cage
shares its label and moves with it. Requires cage to be nested within
treatment; a crossed design cannot be permuted at cage level and is
rejected.}
}
Getting this wrong invalidates the test. If mice were individually
randomised and then re-housed by group, the unit is \code{"mouse"} and cage is a
post-randomisation variable.}

\item{n_perm}{Number of label permutations. Each one refits the mixed model, so
cost scales linearly; 999 is a reasonable default and 199 is enough for a
quick look.}

\item{seed}{Optional integer seed. The caller's RNG state is restored on exit.}
}
\value{
An object of class \code{perm_spec}.
}
\description{
Bundles the settings for \code{\link[=trajectory_permutation_test]{trajectory_permutation_test()}}, following the same
config-helper pattern as \code{\link[=tg_priors]{tg_priors()}} and \code{\link[=tg_mcmc]{tg_mcmc()}} so entry-point
signatures stay readable (CODE_REVIEW.md Round 2 D.2).
}
\examples{
perm_spec()
perm_spec(unit = "cage", n_perm = 199, seed = 42)
}
\seealso{
\code{\link[=trajectory_permutation_test]{trajectory_permutation_test()}}, \code{\link[=tg_priors]{tg_priors()}}, \code{\link[=tg_mcmc]{tg_mcmc()}}
}
