% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bayesian_config.R
\name{tg_priors}
\alias{tg_priors}
\title{Bayesian prior configuration object}
\usage{
tg_priors(
  strength = c("skeptical", "weakly_informative", "informative", "diffuse", "manual"),
  b = NULL,
  intercept = NULL,
  sd = NULL,
  sigma = NULL
)
}
\arguments{
\item{strength}{One of "skeptical" (default), "weakly_informative",
"informative", "diffuse", "manual". See each Bayesian function's
\code{prior_strength} docs for what each mode does.}

\item{b, intercept, sd, sigma}{Optional manual \code{brmsprior} objects or
string specifications, matching the \code{prior_b}, \code{prior_intercept},
\code{prior_sd}, \code{prior_sigma} arguments on \code{bayesian_tumor_growth()} et
al. \code{NULL} (default) means "use the strength-derived default".}
}
\value{
An object of class \code{tg_priors} (a tagged list).
}
\description{
Bundles the five prior-related arguments accepted by every
\verb{bayesian_*} entry point into a single named list so callers don't
have to pass \verb{prior_strength = ..., prior_b = ..., prior_intercept = ..., prior_sd = ..., prior_sigma = ...} five at a time. CODE_REVIEW.md
Round 2 D.2.
}
\details{
The individual \verb{prior_*} arguments on every Bayesian function continue
to work; when a \code{priors = tg_priors(...)} argument is supplied, its
fields take precedence over the legacy arguments. New callers should
prefer the helper:

\preformatted{
fit <- bayesian_tumor_growth(df,
  priors = tg_priors(strength = "weakly_informative", sd = 1),
  mcmc   = tg_mcmc(chains = 4, iter = 800))
}
}
\examples{
tg_priors()
tg_priors(strength = "weakly_informative", sd = 1)
}
\seealso{
\code{\link[=tg_mcmc]{tg_mcmc()}}
}
