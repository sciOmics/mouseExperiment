% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyze_drug_synergy_over_time.R,
%   R/plot_combination_index.R
\name{plot_combination_index}
\alias{plot_combination_index}
\title{Plot Combination Index Over Time}
\usage{
plot_combination_index(
  synergy_summary,
  line_color = "purple",
  title = "Combination Index Over Time",
  x_label = "Days",
  y_label = "Combination Index",
  y_limits = c(0, 2)
)

plot_combination_index(
  synergy_summary,
  line_color = "purple",
  title = "Combination Index Over Time",
  x_label = "Days",
  y_label = "Combination Index",
  y_limits = c(0, 2)
)
}
\arguments{
\item{synergy_summary}{A data frame from the synergy_stats function containing
Time_Point and Combination_Index values over time.}

\item{line_color}{Color of the Combination Index line. Default is "purple".}

\item{title}{Optional custom title for the plot. If NULL, a default title is used.}

\item{x_label}{Label for x-axis. Default is "Days".}

\item{y_label}{Label for y-axis. Default is "Combination Index".}

\item{y_limits}{Optional numeric vector of length 2 specifying y-axis limits.
Default is c(0, 2) to match the common CI scale.}

\item{synergy_results}{Results object from analyze_drug_synergy_over_time function}

\item{custom_title}{Optional custom title for the plot}
}
\value{
A ggplot2 object visualizing CI values over time

A ggplot2 object representing the Combination Index plot.
}
\description{
Creates a line plot visualizing the Combination Index (CI) over time,
with clear indication of synergy (CI < 1) and antagonism (CI > 1) regions.

Creates a line plot visualizing the Combination Index (CI) over time,
with clear indication of synergy (CI < 0.85), additivity (0.85 ≤ CI ≤ 1.15),
and antagonism (CI > 1.15) regions.
}
\examples{
\dontrun{
# First run the analysis
results <- analyze_drug_synergy_over_time(
  df = tumor_data,
  drug_a_name = "Drug A",
  drug_b_name = "Drug B", 
  combo_name = "Drug A + Drug B"
)

# Then create the CI plot
plot_combination_index(results)
}
\dontrun{
# First run the synergy analysis
synergy_results <- synergy_stats(
  df = tumor_data,
  drug_a_name = "Drug A",
  drug_b_name = "Drug B", 
  combo_name = "Combination"
)

# Create the Combination Index plot
plot_combination_index(synergy_results$synergy_summary)

# With custom title and y-axis limits
plot_combination_index(
  synergy_results$synergy_summary,
  title = "Drug Combination Synergy Analysis",
  y_limits = c(0, 1.5)
)
}

}
